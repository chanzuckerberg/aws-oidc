###### Generated using `argus bootstrap` command ######
stack:
  services:
    aws-oidc:
      gateway:
        enabled: true
        paths:
          - path: /
            pathType: Prefix
      image:
        tag: sha-d7f00ae
