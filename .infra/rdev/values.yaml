###### Generated using `argus bootstrap` command ######
stack:
  services:
    aws-oidc:
      gateway:
        oidcProtected: true
        enabled: true
        paths:
          - path: /backend
            pathType: Prefix
      image:
        tag: sha-e009145
