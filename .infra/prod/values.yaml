###### Generated using `argus bootstrap` command ######
stack:
  services:
    aws-oidc:
      image:
        tag: sha-d68cadb
      ingress:
        rules:
          - host: aws-oidc.prod-central.prod.czi.team
